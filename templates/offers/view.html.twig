<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <meta name="description" content="Offer Details" />
        <meta name="author" content="Your Website" />
        <title>Offer Details - Your Website</title>
        <!-- Favicon-->
        <link rel="icon" type="image/x-icon" href="{{ asset('/styles/assets/favicon.ico') }}" />
        <!-- Bootstrap icons-->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" rel="stylesheet" />
        <!-- Core theme CSS (includes Bootstrap)-->
        <link href="{{ asset('/styles/css/styles.css') }}" rel="stylesheet" />
        <style>
            .offer-details-table {
                background-color: #ffffff;
                border-radius: 8px;
                padding: 30px;
                box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
                max-width: 800px;
                margin: auto;
                margin-top: 40px;
            }
            .offer-details-table th {
                background-color: #f8f9fa;
                padding: 10px;
                text-align: left;
                font-weight: bold;
            }
            .offer-details-table td {
                padding: 10px;
                text-align: left;
                vertical-align: top;
            }
           
            .table th, .table td {
                text-align: left;
                padding: 10px;
            }
        </style>
    </head>
    <body class="d-flex flex-column">
        {%
            include "./header/header.html.twig"
        %}
        <main class="flex-shrink-0">
            <!-- Offer Details Section -->
            <section class="py-5 bg-light">
                <div class="container px-5">
                    <div class="offer-details-table">
                        <h2>Offer Information</h2>
                        <div class="d-flex justify-content-center gap-3">
                            <a href="{{ path('offers_list') }}" class="btn btn-secondary ">Back to List</a>
                            <form method="post" action="{{ path('offers_delete', { id: offer.id }) }}" style="display:inline;" onsubmit="return confirm('Are you sure you want to delete this offer?');">
                                <button class="btn btn-danger  ">Delete</button>
                            </form>
                            <button class="btn btn-success  ">Apply</button>
                        </div>
                    </div>
                </div>
            </section>
        </main>
        
        <!-- Scripts-->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
        <script src="{{ asset('/styles/js/scripts.js') }}"></script>
    </body>
</html>
